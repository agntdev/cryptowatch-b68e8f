[
  {
    "name": "Add custom ticker",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Prompt for arbitrary ticker symbol input"
            }
          }
        ],
        "send": {
          "callback": "add_custom_ticker"
        }
      }
    ]
  }
]
